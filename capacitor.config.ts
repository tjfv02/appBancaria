import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appBancaria',
  appName: 'appBancaria',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
